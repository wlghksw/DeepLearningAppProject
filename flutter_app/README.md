# AI 스마트폰 상태 검사 앱

React 앱(`ai-smartphone-inspector`)을 Flutter로 포팅한 간단한 AI 기반 스마트폰 검사 앱입니다.

## 기능

- 📸 4개 이미지 업로드 (전면, 후면, 좌측, 우측)
- 🔋 배터리 성능 입력
- 🤖 Gemini AI를 통한 자동 품질 분석
- 📊 등급(S, A, B, C, D) 및 상세 리포트 표시

## 설정

### 1. Gemini API 키 설정

프로젝트 루트에 `.env` 파일을 생성하거나, 실행 시 환경 변수로 설정:

```bash
# macOS/Linux
export GEMINI_API_KEY="your-api-key-here"
flutter run

# 또는 dart run 시
dart run --dart-define=GEMINI_API_KEY=your-api-key-here
```

### 2. 의존성 설치

```bash
flutter pub get
```

### 3. 실행

```bash
# 웹에서 실행 (빠른 테스트)
flutter run -d chrome

# iOS 시뮬레이터
flutter run -d ios

# macOS 데스크톱
flutter run -d macos
```

## 사용 방법

1. 앱을 실행합니다
2. 전면, 후면, 좌측, 우측 이미지를 각각 업로드합니다
3. 배터리 성능(%)을 입력합니다
4. "AI 검사 시작" 버튼을 클릭합니다
5. AI 분석 결과를 확인합니다

## 프로젝트 구조

```
lib/
  ├── main.dart                    # 앱 진입점
  ├── screens/
  │   └── inspection_screen.dart   # 메인 검사 화면
  ├── services/
  │   └── gemini_service.dart      # Gemini AI 연동 서비스
  ├── models/
  │   └── inspection_report.dart   # 검사 결과 모델
  └── theme/
      └── app_theme.dart           # 앱 테마
```

## 참고

- Gemini API 키는 [Google AI Studio](https://makersuite.google.com/app/apikey)에서 발급받을 수 있습니다.
- 이미지는 갤러리에서 선택합니다 (카메라 촬영은 추후 추가 가능).
